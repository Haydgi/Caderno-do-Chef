# ═══════════════════════════════════════════════════════════
# Caderno do Chef - Configurações do Servidor
# ═══════════════════════════════════════════════════════════

# ─────────────────────────────────────────────────────────
# CONFIGURAÇÕES DO SERVIDOR
# ─────────────────────────────────────────────────────────
# HOST: Use 0.0.0.0 para permitir acesso de outros dispositivos na rede
# Use localhost ou 127.0.0.1 apenas para acesso local
HOST=0.0.0.0

# PORT: Porta em que o servidor backend irá rodar
PORT=3001

# ─────────────────────────────────────────────────────────
# CONFIGURAÇÕES DO BANCO DE DADOS
# ─────────────────────────────────────────────────────────
# Host do banco de dados (geralmente localhost)
DB_HOST=localhost

# Usuário do banco de dados
DB_USER=root

# Senha do banco de dados (deixe vazio se não houver senha)
DB_PASSWORD=

# Nome do banco de dados
DB_NAME=caderno_chef

# ─────────────────────────────────────────────────────────
# SEGURANÇA
# ─────────────────────────────────────────────────────────
# Chave secreta para JWT (IMPORTANTE: Altere em produção!)
# Gere uma chave forte: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
SECRET_JWT=your-secret-key-here-change-in-production-use-strong-random-string

# ─────────────────────────────────────────────────────────
# CONFIGURAÇÕES DE EMAIL (Opcional)
# ─────────────────────────────────────────────────────────
# Necessário apenas se for usar recuperação de senha por email

# Serviço de email (gmail, outlook, etc.)
EMAIL_SERVICE=gmail

# Email remetente
EMAIL_USER=seu-email@gmail.com

# Senha do email ou senha de aplicativo
EMAIL_PASSWORD=sua-senha-aqui

# ─────────────────────────────────────────────────────────
# OUTRAS CONFIGURAÇÕES (Opcional)
# ─────────────────────────────────────────────────────────

# Ambiente de execução (development, production)
NODE_ENV=development

# Ativar logs detalhados (true, false)
DEBUG=false

# ─────────────────────────────────────────────────────────
# INSTRUÇÕES
# ─────────────────────────────────────────────────────────
# 1. Copie este arquivo e renomeie para .env
# 2. Preencha as informações do banco de dados
# 3. Gere uma chave secreta forte para SECRET_JWT
# 4. Configure o email se for usar recuperação de senha
# 5. Não compartilhe este arquivo! Ele contém informações sensíveis
# 6. Adicione .env ao .gitignore
